# Agents

## Rules

- Always commit changes when done.
- Always run `cargo fmt` before committing.
- Never add LLM signatures to commits (e.g. "Co-Authored-By: ...").
- Follow the existing commit message style: short imperative summary line, optional blank line and body explaining the why.
- Never leave compiler warnings. Run `cargo build` and fix all warnings before committing.
